# Agent Rules

- Never use npm for this project as package manager, always use bun.
- Never push changes to github, even after committing.
- Follow conventional commits for committing within the repository, always add scopes to the commits e.g ```feat(scope): comment here```.
